Looking for ORT configuration in the following file:
        /home/ort/.ort/config/config.yml

Usage: ort [<options>] <command> [<args>]...

Error: no such option --package-scanners. (Possible options: --package-types, --project-scanners, --scanners)
❌ Scan result missing or empty.
❌ Pipeline failed (exit code 1)
mypc@mypc-VirtualBox:~/FOSShub$ 

header "ORT Scan"
info "Running ORT scan (ScanCode; using persisted ORT cache)..."
docker run --rm -u "$(id -u):$(id -g)" \
  -v "$PROJECT_DIR":/project \
  -v "$OUTPUT_DIR":/ort/data \
  -v "$CONFIG_DIR":/home/ort/.ort/config \
  -v "$ORT_CACHE_DIR":/home/ort/.cache \
  -e JAVA_TOOL_OPTIONS="$JAVA_TOOL_OPTIONS_VALUE" \
  "$ORT_IMAGE" scan \
  --ort-file "/ort/data/analyzer-result/$(basename "$ANALYZE_RESULT")" \
  --package-types "PROJECT,PACKAGE" \
  --project-scanners "ScanCode,Licensee" \
  --package-scanners "ScanCode" \
  --skip-excluded \
  -o /ort/data/scanner-result
